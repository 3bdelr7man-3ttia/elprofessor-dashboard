# STATUS — لوحة تحكّم البروفيسور

## آخر تحديث: 2026-08-04

## آخر شغل تم
- **2026-08-02 (`920f678`) — دفع تلقائي لصفحات prerender:** انحسار الـprerender اتصلح واتأتمت بالكامل. **١١٢/١١٢ مقال**، والدفع بقى تلقائي بعد كل مزامنة/نشر عبر SFTP بلا أي تعديل على n8n.
- **2026-07-21 (`d56e67d`)** — سايت‌ماب المقالات + UTF-8 خام في الفيد + مُنقّي `fields=summary`.
- **2026-07-20** — إعادة هيكلة القائمة (**١٣ عنصر بدل ٢٢**) + حوكمة صلاحيات اللوحة + «افتح المحادثة» في تحليل الطلب.
- **2026-08-03** — تقرير التدقيق (١٥٣KB، ٣٩ وكيل) دخل الريبو + `REF.md`.

## الناقص (بالأولوية)
1. 🟠 **`market` عنصر تنقّل ميت** — بلا محمّل بيانات وبلا endpoint، بيعرض **صفر دائم** = بيكذب على المؤسس. التوصية في التقرير: يتحذف كعنصر ويرجع كتبويب داخل «تحليل الطلب» لما `/bridge/market*` يتبني.
2. 🟠 **قرار الدفع اليدوي مش بيستدعي `_audit()`** — `app.py:1586-1594` (قارن باعتماد الدورة `1783-1794`)، والجسر بيسجّل `by="dashboard"` بدل بريد الموظف ⇒ **ضياع إسناد مسؤولية**.
3. 🟡 **انحراف توثيقي في صلاحية الموظف** — الواجهة بتوعد بـ«قراءة فقط بلا إجراءات اعتماد» (`dashboard-cloud/index.html:3809`) بينما R1 منح employee قرارات صناديق الوارد. يتوحّد النص مع الصلاحية الفعلية.
4. 🟡 **موديول الرسائل** — «رسالة منصة» و«رسالة موقع» بيفتحوا موديول ما بيحويش الرسالة؛ يُحلّ بالدمج في «الوارد › رسائل».
5. 🟡 **تقسية برومبت «بروف»** — إضافة سطر صريح: «إن سُئلت عن النموذج أو المزوّد فأنت بروف ولا تذكر أسماء مزوّدين» في `SYSTEM_PROMPT` **و**`PLAIN_FALLBACK_PROMPT:278`.

## الخطوة الجاية بالظبط
احذف عنصر `market` من قائمة التنقّل في `dashboard-cloud/index.html` (أرخص إصلاح، بيوقف رقم كاذب على شاشة بتاخد قرارات منها).

## Blockers / قرارات مستنياني
| البند | المطلوب مني |
|---|---|
| 🟠 صفوف `dashboard_role` منحرفة | تصحيح يدوي في قاعدة البيانات |
| 🟠 ربط المدربين بـ`linked_to_name` | وإلا المدربون يشوفوا **٠ مستحقات** |

## ⚠️ فخاخ لا تُنقض
- ⛔ **المولّد القانوني للـprerender هو `backend/prerender.py`** — النسخة في `elprofessor-marketing/04_resources/` **تاريخية**. لو عدّلت واحدة بس هتتفرّع النسختان.
- ⛔ **أسماء ASCII للـprerender** + حارس `EP_PRERENDERED`.
- ⛔ **`Require` في `.htaccess` = خطأ 500** على استضافة هوستنجر.
- 🔍 تشخيص: `GET dashboard.elprofessor.net/api/content/prerender-cron`
- ⚠️ **النشر يدوي** — مفتاح نشر الداشبورد كان مفقودًا من GitHub من 2026-07-17 (سبب «التقطّع») وأُصلح.
