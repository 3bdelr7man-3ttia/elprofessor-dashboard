# STATUS — لوحة تحكّم البروفيسور

## آخر تحديث: 2026-08-13

## آخر شغل تم
- **2026-08-12 (`caa8a4e`) — «السوق» اتبنى بدل ما يتحذف.** التوصية القديمة كانت «احذف العنصر»؛ اللي حصل إن الجسر اتبنى على الطرفين والقسم رجع كامل: الحارات الأربع الحقيقية (`request` · `public_service` · `course_request` · `training_offer`) بنفس محاور البورد الحيّ، فلترة `need_kind`/`status`/بحث، درج الطلب بعروضه، **إغلاق وإخفاء منسوبين ومسجَّلين في `_audit`** (`app.py:2749` و`2771`)، ولوحة «الطلب الحقيقي» من `/api/platform-market-demand`. معاه مراجعة مواضيع الأعضاء وتصنيفها الفئوي + تعطيل المدرّب الآمن. ٣٧٥ سطر اختبار جديد (`backend/test_market_bridge.py`).
  - **✅ متحقَّق النهاردة:** الجسر **حيّ** على `api.elprofessor.net` (`/api/bridge/market/requests` و`/demand` بيرجّعوا **401** = منشور ومحمي، مش 404)، والقسم **منشور فعلًا** على `dashboard.elprofessor.net`. يعني بطاقة «الجسر لسه ما اتنشرش» مالهاش لازمة دلوقتي والشاشة بتقرا بيانات حقيقية.
- **2026-08-11 (`ef748dd`)** — بوكس صنارة الليدات في قالب المقالات المُسبقة، مركّب **خارج `#art`** علشان ينجو من الهيدريشن.
- **2026-08-09 (`dcb5105`)** — أحدث الأسئلة أولًا + تنسيق الدورات + أرقام حقيقية + «إضافة كتاب» + تشخيص المحتوى + إدارة التعليقات.
- **2026-08-08 (`b8bbf32`)** — صدق دفتر الإيرادات: `occurred_at` من الحدث نفسه، **عملات الخليج ما بتتصفّرش** (تروح `amount_original` + `needs_fx`)، وربط الدورة **بالمعرّف** بدل مطابقة العنوان. ٢٥٠ سطر اختبار.
- ✅ **بند اتقفل بالمراجعة:** تقسية برومبت «بروف» **معمولة أصلًا** في ريبو المنصة — `backend/routes/legal_search.py` سطر ١٩٧ (`SYSTEM_PROMPT`) وسطر ٢٩٠ (`PLAIN_FALLBACK_PROMPT`)، commit `7e19e78` من 2026-07-20، ومغطّاة باختبار `test_anti_fabrication_a5.py`. كانت مكتوبة في القائمة بالغلط.
- الشجرة نضيفة، `main` = `origin/main` بالظبط، مفيش شغل غير مكوميت ولا فرع شغّال دلوقتي.

## الناقص (بالأولوية)
1. 🟠 **قرار الدفع اليدوي لسه بلا `_audit()`** — `backend/app.py:1894-1902` بروكسي صافي من غير سطر تسجيل، والجسر بيسجّل `by="dashboard"` بدل بريد الموظف ⇒ **ضياع إسناد مسؤولية على أخطر إجراء مالي**. النمط الصح بقى موجود جنبه في نفس الملف (`market.close_request`) فالنسخ مباشر.
2. 🟠 **`relane` مبني على المنصة ومش موصّل هنا** — endpoint نقل الطلب المُخزَّن في مسار غلط اتشحن في المنصة (`11ab011`، 2026-08-12، idempotent وعكسي وبيرفض طلبًا عليه عروض) لكن مفيش له أي استدعاء في `backend/app.py` ولا `dashboard-api.js` ⇒ طلب في حارة غلط مفيش منه مخرج من الكوكبيت.
3. 🟡 **موديول الرسائل لسه مفصول** — الوارد بيقرا «رسالة من المنصة» من `/platform-messages`، بينما موديول «الرسائل» بيحمّل `/api/messages` (نموذج تواصل الموقع) بس ⇒ زرار «فتح الرسائل» بيوَدّي شاشة مفيهاش الرسالة. اتخفّف جزئيًا (الدرج بقى يعرض مقتطف + زرار «اعتبرها منتهية» عبر `resolve`)، لكن الدمج لسه ناقص.
4. 🟡 **انحراف توثيقي في صلاحية الموظف** — `dashboard-cloud/index.html:4388` لسه بيوعد «قراءة فقط… بلا إجراءات اعتماد» بينما الموظف فعلًا بياخد قرارات صناديق الوارد. يتوحّد النص مع الصلاحية الحقيقية.
5. 🟡 **صفوف `needs_fx` بلا شاشة تسوية** — المبيعات بالعملات الخليجية بقت تتحفظ بأمانة (كويس)، لكن الإعدادات فيها **سعر صرف واحد للدولار بس** (`index.html:3753`) ⇒ الصفوف دي هتفضل «بانتظار تسوية» ومش داخلة في أي تقرير.
6. 🟡 **«نسخة احتياطية الآن» و«المصادقة الثنائية»** لسه أزرار **«قريبًا»** معطّلة في الإعدادات (`index.html:3734-3735`) — وده متشابك مع بلوكر نسخ Mongo تحت.

## الخطوة الجاية بالظبط
في `backend/app.py:1894` (`platform_manual_payment_decide`): خزّن رد `_platform_proxy` في متغيّر، وبعد نجاحه ضيف
`_audit(f'payment.manual_{action}', target=payment_id, meta={'note': body.get('admin_note') or ''})`
ثم رجّع الرد — نسخ حرفي لنمط `market.close_request` (`app.py:2749`). ~٤ أسطر، وبيقفل أخطر ثغرة إسناد فاضلة.

## Blockers / قرارات مستنياني
| البند | المطلوب مني |
|---|---|
| 🔴 نسخ Mongo احتياطي | لسه **صفر** — وزرار النسخة في اللوحة نفسه «قريبًا» |
| 🟠 صفوف `dashboard_role` منحرفة | تصحيح يدوي في قاعدة البيانات |
| 🟠 ربط المدربين بـ`linked_to_name` | وإلا المدربون يشوفوا **٠ مستحقات** |
| 🟡 سعر صرف عملات الخليج | تحدّد السعر أو آلية التسوية عشان صفوف `needs_fx` تتحوّل لأرقام في التقارير |

## ⚠️ فخاخ لا تُنقض
- ⛔ **المولّد القانوني للـprerender هو `backend/prerender.py`** — النسخة في `elprofessor-marketing/04_resources/` **تاريخية**. لو عدّلت واحدة بس هتتفرّع النسختان.
- ⛔ **أسماء ASCII للـprerender** + حارس `EP_PRERENDERED`.
- ⛔ **`Require` في `.htaccess` = خطأ 500** على استضافة هوستنجر.
- ⛔ **مفيش `/flag-test` لطلب سوق بعينه** — وسم «تجريبي» ملك الكنسة الجماعية (`POST /api/bridge/flag-test-data`) وبيتعمل بالإيميل. الرافعة الوحيدة على طلب مفرد هي `/hide` (قابلة للرجوع ومنسوبة). الاستدعاء القديم كان بيرجّع 404 والواجهة تلوم «جسر مش منشور» وهو منشور.
- ⛔ **بوكس الليدات في قالب المقال لازم يفضل خارج `#art`** وإلا الهيدريشن بيمسحه.
- 🔍 تشخيص: `GET dashboard.elprofessor.net/api/content/prerender-cron`
- ⚠️ **النشر يدوي** — مفتاح نشر الداشبورد كان مفقودًا من GitHub من 2026-07-17 (سبب «التقطّع») وأُصلح.
