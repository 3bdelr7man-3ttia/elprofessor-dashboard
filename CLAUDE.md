# CLAUDE.md — لوحة تحكّم البروفيسور

## المشروع بيعمل إيه
**كوكبيت المؤسس** — الشاشة اللي بتُدار منها الشركة: صناديق الوارد (طلبات/رسائل/مدفوعات
يدوية/طلبات انضمام/مراجعة مواضيع)، تحليل الطلب، إدارة المحتوى (CMS للموقع التسويقي)،
المقاييس المالية، واعتمادات الدورات والخبراء. كمان بيولّد **صفحات المقالات المُسبقة العرض**
(prerender) للزواحف اللي ما بتنفّذش JavaScript.

## الستاك
- **Frontend:** Vite — يُخدَم من `dashboard-cloud/` عبر `/dashboard-api.js` (**مش** `/dashboard-cloud/*`).
- **Backend:** Python + SQLite (`dashboard_backend.py` + `backend/`).
- **الوجهة:** `dashboard.elprofessor.net` · Coolify uuid `dgg8o04cwg0cckwc4g0wkwcs`.
- **الجسر للمنصة:** metrics-secret.

## قرارات لا تُنقض
- ⛔ **`backend/prerender.py` هو المولّد القانوني الوحيد.** النسخة في `../elprofessor-marketing/04_resources/prerender.py` تاريخية للتشغيل الطارئ. **عدّل هنا بس.**
- ⛔ **أسماء ASCII** للملفات المولّدة + حارس `EP_PRERENDERED` (يمنع الكتابة فوق صفحات حقيقية).
- ⛔ **متحطش `Require` في `.htaccess`** — بيرجّع 500 على هوستنجر.
- ⛔ **القائمة ١٣ عنصر** بعد إعادة الهيكلة (2026-07-20) — مترجعش الـ٢٢.
- ✅ **كل كتابة مالية محمية بصلاحية** بعد تحصين 2026-07-20 (قبله كانت مفتوحة لأي مُصادَق).
- ✅ **مطابقة الاسم بالتساوي لا بالاحتواء** — حساب اسمه «ا» كان بيقرا الدفتر كله.
- 📄 ملفات خطة الإطلاق (`03_LAUNCH_FIX_PLAN` / `04_LAUNCH_FIX_PROMPTS`) مكانها ريبو `elprofessor` — شوف `REF.md`.

## نشر
```bash
git push origin main
curl "http://72.60.104.30:8000/api/v1/deploy?uuid=dgg8o04cwg0cckwc4g0wkwcs&force=true"
```

## علاقات
- **`../elprofessor`** — المنصة الأم (نفس الهوية، مفتاحها EMAIL).
- **`~/elprofessor-site`** — الموقع التسويقي بيقرا **CMS feed** من هنا، وصفحات المقالات المُسبقة بتُدفع من هنا بـSFTP.
- 📊 الصورة الكاملة: `../PROJECTS.md`
